name: pages match the contract
on: [push, pull_request]
jobs:
  build-is-current:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 build.py
      - name: Check generated documentation
        run: git diff --exit-code -- docs/
